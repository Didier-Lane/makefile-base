## Makefile Base

>A project skeleton using Makefiles

### About

This project is about providing the basic structure for operations abstracted with Make recipes

It manages the automatic creation of an `.env` file for storing environment variables and a basic helper
